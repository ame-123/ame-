# 路由小模型：微调总结与接入

口径：旅行客服主链路在 `travel_agent`。路由小模型的造数、本地测评、LoRA 在 `projects/knowledge-assistant/intent-finetune`。

**分类已改为本地私有模型，不再走硅基流动 Qwen3.5-4B。** 话术仍用 DeepSeek。

- 分类：`AGENT_CLASSIFIER_MODEL=Qwen/Qwen2.5-3B-Instruct`
- 地址：`AGENT_CLASSIFIER_BASE_URL=http://127.0.0.1:8000/v1`
- 服务：RWKV7 里 `python -m llamafactory.cli api train/api_qwen25_3b.yaml`
- Adapter：SFT 用 `outputs/sft-qwen25-3b-ctx-lora`；DPO 训完后改 `outputs/dpo-qwen25-3b-ctx-lora`

`langchain12` 没有 Torch，必须起 OpenAI 兼容服务。服务没起来时分类失败，回退 `classify_intent`（只看当前句）。

旧的单轮 464/124 与 `sft-qwen25-3b-lora` **作废**。`input` 含 `[session_state]` 与最近 8 条 `[recent_dialogue]`，与 `route_session_hint` 一致。SFT 后接 DPO：同一短句，chosen=结合历史的正确意图，rejected=只看当前句或粘住 `recent_intent` 的错意图。
